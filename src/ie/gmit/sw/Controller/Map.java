package ie.gmit.sw.Controller;

import ie.gmit.Model.Afferent;
import java.util.HashMap;

public class Map {
	/**
	 *  Supposed to create a map of the afferent results and the class name
	 *  
	 *  @author neilb
	 *  
	 */
	/*
	private Map<Class, Afferent> classMap = new HashMap<>();

    public int size() {
        return classMap.size();
    }

    public boolean isEmpty() {
        return classMap.isEmpty();
    }

    public boolean containsKey(Object key) {
        return classMap.containsKey(key);
    }

    public boolean containsValue(Object value) {
        return classMap.containsValue(value);
    }

    public void clear() {
        classMap.clear();
    }
*/
   }
